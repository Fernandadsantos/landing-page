export interface TitleBoxParams {
  title: string;
  subtitle: string;
}

export interface RedirectCardParams {
  children: React.ReactNode;
  titleTop: string;
  titleBottom: string;
  link: string;
  theme: string;
}
